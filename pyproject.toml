[project]
name = "doc-query-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.20.0",
    "asyncpg>=0.31.0",
    "cli-document-processor",
    "fastapi[standard]>=0.141.1",
    "psycopg2-binary>=2.9.13",
    "sqlalchemy>=2.0.53",
]

[tool.uv.sources]
cli-document-processor = { path = "../CLI Document Processor", editable = true }
