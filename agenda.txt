Phase 2 project : 
Build a FastAPI app that:
    • Has endpoints for: document upload, text embedding generation, vector search
    • Stores documents + embeddings in PostgreSQL with pgvector
    • Calls OpenAI/embedding APIs asynchronously with httpx
    • Has Pydantic models for all inputs/outputs
    • Dockerized with docker-compose (FastAPI + Postgres + pgvector)
    • Basic rate limiting (middleware or dependency)
    • Push to GitHub. Include a docker-compose up command in README.
    
